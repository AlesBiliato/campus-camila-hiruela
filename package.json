{
  "name": "campus-camila-hiruela",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run sass && vite build",
    "preview": "vite preview",
    "sass": "sass style.scss css/estilos.css --no-source-map",
    "sass:watch": "sass --watch style.scss:css/estilos.css --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.99.0",
    "vite": "^7.3.2"
  }
}
